
갤러리에서 아래  progress 진행바 만들기 - 윤정님


#함수 인수, 매개변수  
인수 : argument  => 함수를 호출하면서 전달되는 값 : 호출한 함수로 전달되는 값을 인수라고 한다
매개변수 :parameter  => 인수로 전달된 값을 함수에서 사용할 수 있게 저장하는 변수를 매개변수라고 한다

#함수반환
값을 반환할 수 있다 . return문

#중첩함수 :
자바스크립트는 함수 내에서 또 다른 함수를 선언할 수 있다
특정 함수 내부에서 선언된 함수를 중첩함수라고 한다

function greeting(){
    function greetingWidthName( name ) {
     console.log( `hello :  ${name}`);
    }

    let name ="우주연";
     grretingWidthName(name);

}

중첩 함수를 많이 두면 가독성을 헤치는 단점이 있으나 적절히 활용하면 
함수내에서 서로 역할을 분담할 수 있어 중복코드를 방지하는데 도움이 된다


#함수와 호이스팅
호이스팅: 프로그램에서 변수나 함수를 호출하거나 접근하는 코드가 함수선언보다 위에 있음에도 
불구하고 마치 선언 코드가 위에 있는 것처럼 동작하는 자바스크립트만의 독특한 기능입니다


#함수표현식
자바스크립트는 함수 선언 말고도 함수를 만드는 또 다른 방법이 있다
바로 함수표현식을 이용하는 방법

함수표현식이란 함수를 생성하고 변수에 값으로 저장하는 방법



